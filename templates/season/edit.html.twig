{% extends 'base.html.twig' %}

{% block title %}Modifier saison{% endblock %}

{% block body %}
    <a class="btn btn-success mb-3" href="{{ path('admin_seasonAdmin') }}">Liste des saison admin</a>

<div class="container">
    <div class="row">
        <div class="col-md-6 offset-md-3">
            <div class="text-center mb-4">
                <h1 class="mb-0">Modifier saison</h1>
            </div>

    {{ include('season/_form.html.twig', {'button_label': 'Enregistrer'}) }}
        </div>
    </div>
</div>
{% endblock %}
