{% extends 'base.html.twig' %}

{% block title %}Ajouter un film{% endblock %}

{% block body %}
    <a class="btn btn-primary btn-menu" href="{{ path('admin_index') }}">Menu administrateur</a>

    <div class="container py-4">
        <div class="row">
            <div class="col-md-6 offset-md-3">
                <div class="text-center mb-4">
                    <h1 class="title">Ajouter un nouveau film</h1>
                </div>
                    {{ include('film/_form.html.twig') }}
                    </div>
                </div>
            </div>
{% endblock %}
