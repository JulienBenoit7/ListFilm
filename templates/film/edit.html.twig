{% extends 'base.html.twig' %}

{% block title %}Modifier {{ film.title }}{% endblock %}

{% block body %}
    <a class="btn btn-primary mb-3" href="{{ path('admin_filmAdmin') }}">Liste des films admin</a>

    <div class="container">
        <div class="row">
            <div class="col-md-6 offset-md-3">
                <div class="text-center mb-4">
                    <h1 class="mb-0">Modifier {{ film.title }}</h1>
                </div>
                {{ include('film/_form.html.twig', {'button_label': 'Enregistrer'}) }}
            </div>
        </div>
    </div>
{% endblock %}
